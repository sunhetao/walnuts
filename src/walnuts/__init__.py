from .config_manager import v
from .api_decorator import request_mapping, add_headers, add_header, Requester, Method, before_request, RequestParams

RequestMapping = request_mapping
BeforeRequest = before_request
